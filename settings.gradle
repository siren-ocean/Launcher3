pluginManagement {
    repositories {
        maven { url = uri("https://maven.aliyun.com/repository/gradle-plugin") }
        maven { url = uri("https://maven.aliyun.com/repository/google") }
        maven { url = uri("https://maven.aliyun.com/repository/public") }
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven { url = uri("https://maven.aliyun.com/repository/google") }
        maven { url = uri("https://maven.aliyun.com/repository/public") }
        flatDir {
            dirs("${rootDir}/libs")
        }
        google()
        mavenCentral()
    }
}

include ':Filter'
include ':iconloaderlib'
include ':animationlib'
include ':viewcapturelib'
include ':animation'
include ':shared'
include ':unfold'
include ':common'
include ':log'
include ':Shell'
include ':plugin'
include ':plugin_core'
include ':SettingsTheme'

include ':biometrics'
project(':biometrics').projectDir = new File('shared/biometrics')